/* ── Individual Project Row ── */
.project-row {
  border-radius: 18px;
  background: var(--glass-bg);
  border: 1px solid var(--glass-border);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  box-shadow: var(--shadow-sm);
  overflow: hidden;
  transition: border-color var(--transition-normal), box-shadow var(--transition-normal),
              opacity 0.3s ease, transform 0.3s ease;
}

.project-row:hover {
  border-color: var(--glass-border-hover);
  box-shadow: var(--shadow-md), 0 0 18px var(--glow-color);
}

/* ── Compact summary row (always visible, clickable) ── */
.proj-row-summary {
  display: flex;
  align-items: center;
  gap: 1rem;
  padding: 1rem 1.25rem;
  cursor: pointer;
  position: relative;
  transition: background var(--transition-fast);
  user-select: none;
}

.proj-row-summary::before {
  content: '';
  position: absolute;
  inset: 0;
  background: radial-gradient(600px circle at var(--mouse-x, -200px) var(--mouse-y, -200px), rgba(255,255,255,0.04), transparent 40%);
  pointer-events: none;
  border-radius: inherit;
}

.proj-row-summary:hover {
  background: rgba(255,255,255,0.03);
}

[data-theme="light"] .proj-row-summary:hover {
  background: rgba(0,0,0,0.02);
}

/* Left: icon + title/tagline */
.proj-row-left {
  display: flex;
  align-items: center;
  gap: 0.9rem;
  flex: 1;
  min-width: 0;
}

.proj-row-info {
  display: flex;
  flex-direction: column;
  min-width: 0;
}

.proj-row-info .project-title {
  font-family: var(--font-heading);
  font-weight: 700;
  font-size: 1rem;
  color: var(--text-primary);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.proj-row-info .project-tagline {
  font-size: 0.78rem;
  color: var(--text-muted);
  font-weight: 500;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  margin: 0;
}

/* Right: category pill + skills + chevron */
.proj-row-right {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  flex-shrink: 0;
}

.project-category-pill {
  font-family: var(--font-heading);
  font-size: 0.68rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  color: var(--accent-color);
  background: rgba(237, 188, 135, 0.1);
  border: 1px solid rgba(237, 188, 135, 0.3);
  padding: 0.22rem 0.7rem;
  border-radius: 50px;
  white-space: nowrap;
}

.proj-row-skills {
  display: flex;
  gap: 0.4rem;
  flex-wrap: nowrap;
}

/* Expand/collapse chevron button */
.proj-expand-btn {
  width: 32px;
  height: 32px;
  border-radius: 50%;
  border: 1px solid var(--glass-border);
  background: var(--glass-bg);
  color: var(--text-muted);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 0.75rem;
  flex-shrink: 0;
  cursor: pointer;
  transition: all var(--transition-normal);
}

.proj-expand-btn:hover {
  border-color: var(--glass-border-hover);
  color: var(--text-primary);
  background: var(--card-hover-bg);
}

.project-row.expanded .proj-expand-btn {
  background: var(--accent-gradient);
  border-color: transparent;
  color: #fff;
  box-shadow: 0 4px 12px var(--glow-color);
}

.project-row.expanded .proj-expand-btn i {
  transform: rotate(180deg);
}

.proj-expand-btn i {
  transition: transform var(--transition-normal);
}

/* ── Icon box ── */
.project-icon-box {
  width: 40px;
  height: 40px;
  min-width: 40px;
  border-radius: 11px;
  background: var(--accent-gradient);
  display: flex;
  align-items: center;
  justify-content: center;
  color: #ffffff;
  font-size: 1rem;
  box-shadow: 0 4px 12px var(--glow-color);
  transition: transform var(--transition-normal);
}

.proj-row-summary:hover .project-icon-box {
  transform: scale(1.08) rotate(6deg);
}

/* ── Expandable detail panel ── */
.proj-detail-panel {
  max-height: 0;
  overflow: hidden;
  transition: max-height 0.45s cubic-bezier(0.4, 0, 0.2, 1);
}

.project-row.expanded .proj-detail-panel {
  border-top: 1px solid var(--glass-border);
}

.proj-detail-inner {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 0;
}

/* Gallery side */
.proj-detail-gallery {
  position: relative;
  overflow: hidden;
  border-right: 1px solid var(--glass-border);
  min-height: 200px;
  max-height: 300px;
}

.proj-detail-gallery img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
  transition: transform 0.5s ease, opacity 0.2s ease;
}

.proj-detail-gallery:hover img {
  transform: scale(1.04);
}

.proj-gallery-overlay {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  padding: 0.75rem 1rem;
  background: linear-gradient(to top, rgba(7,10,19,0.85) 0%, transparent 100%);
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 0.5rem;
}

/* Content side */
.proj-detail-content {
  padding: 1.25rem 1.5rem;
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

.proj-detail-content .project-desc {
  font-size: 0.85rem;
  color: var(--text-secondary);
  line-height: 1.65;
  margin: 0;
}

/* Components checklist */
.proj-detail-list {
  list-style: none;
  padding: 0;
  margin: 0;
  display: flex;
  flex-direction: column;
  gap: 0.4rem;
}

.proj-detail-list li {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  font-size: 0.82rem;
  color: var(--text-secondary);
}

.proj-detail-list li i {
  font-size: 0.6rem;
  color: var(--accent-color);
  flex-shrink: 0;
}

/* Skill tags (shared, used in both row and detail) */
.project-skill-tag {
  background: rgba(0, 0, 0, 0.2);
  border: 1px solid var(--glass-border);
  color: var(--text-secondary);
  padding: 0.2rem 0.6rem;
  border-radius: 50px;
  font-size: 0.68rem;
  font-weight: 500;
  white-space: nowrap;
  transition: all var(--transition-fast);
}

[data-theme="light"] .project-skill-tag {
  background: rgba(0, 0, 0, 0.04);
}

.proj-row-summary:hover .project-skill-tag {
  border-color: var(--glass-border-hover);
  color: var(--text-primary);
}

/* Footer links */
.project-card-footer {
  display: flex;
  gap: 0.5rem;
  flex-wrap: wrap;
  align-items: center;
  padding-top: 0.75rem;
  border-top: 1px solid var(--glass-border);
  margin-top: auto;
}

.project-link {
  font-family: var(--font-heading);
  font-weight: 600;
  font-size: 0.78rem;
  display: inline-flex;
  align-items: center;
  gap: 0.4rem;
  padding: 0.45rem 0.95rem;
  border-radius: 50px;
  transition: all var(--transition-fast);
}

.project-link-demo {
  background: var(--accent-gradient);
  color: #ffffff !important;
  box-shadow: 0 4px 14px var(--glow-color);
}

.project-link-demo:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 20px var(--glow-color);
}

.project-link-github {
  background: var(--glass-bg);
  border: 1px solid var(--glass-border);
  color: var(--text-secondary) !important;
}

.project-link-github:hover {
  border-color: var(--glass-border-hover);
  color: var(--text-primary) !important;
  transform: translateY(-2px);
  background: var(--card-hover-bg);
}

.project-link i {
  transition: transform var(--transition-fast);
  font-size: 0.8rem;
}

.project-link-demo:hover i { transform: translate(2px, -2px); }
.project-link-github:hover i { transform: scale(1.2); }

/* No-gallery case: detail panel is single column */
.proj-detail-inner:not(:has(.proj-detail-gallery)) {
  grid-template-columns: 1fr;
}
